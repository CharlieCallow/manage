// Placeholder. CLAUDE.md §11 says TS types are generated from Pydantic via
// datamodel-code-generator. That pipeline is wired up in a later phase;
// for now the desktop app owns its own copy of the wire types in
// apps/desktop/src/preload/types.ts.
export {};
